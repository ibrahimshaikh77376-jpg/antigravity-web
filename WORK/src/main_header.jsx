import React, { useState, useEffect, useRef } from 'react';
import ReactDOM from 'react-dom/client';

// Icons are loaded via CDN (lucide global)
const { User, Lock, Users, PlusCircle, LayoutDashboard, LogOut, Save, Search, Table, Eye, EyeOff, Edit, X } = window.lucide || {};
